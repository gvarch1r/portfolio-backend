# Web framework
fastapi>=0.109,<0.116
uvicorn[standard]>=0.27,<0.32

# Database
sqlalchemy>=2.0.25
aiosqlite>=0.19
# asyncpg — только для PostgreSQL, ставится в Docker

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0,<5  # passlib несовместим с bcrypt 5.x

# Validation & env (2.12+ для Python 3.14)
pydantic[email]>=2.12
pydantic-settings>=2.1

# AI / LLM
openai>=1.12,<2

# Testing
pytest>=7.4,<9
pytest-asyncio>=0.23,<0.24
httpx>=0.26,<0.28
